includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 6
    tmpDir: var/phpstan

    paths:
        - app/
        - database/
        - tests/

    treatPhpDocTypesAsCertain: false
    reportUnmatchedIgnoredErrors: false

    excludePaths:
        - database/migrations/*
        - database/seeders/*

    ignoreErrors:
        # Сюда добавлять исключения по мере необходимости
