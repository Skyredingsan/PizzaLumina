
services:
  php:
    build:
      target: prod
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
    volumes: []
    restart: unless-stopped

  nginx:
    restart: unless-stopped

  postgres:
    restart: unless-stopped

  redis:
    restart: unless-stopped

  rabbitmq:
    restart: unless-stopped
